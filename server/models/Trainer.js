export default class Trainer {
  constructor(
    id,
    name,
    teams,
    badge,
    email,
    friends,
    pokemon,
    password,
  ) {
    this.id = id;
    this.name = name;
    this.teams = teams;
    this.badge = badge;
    this.email = email;
    this.friends = friends;
    this.pokemon = pokemon;
    this.password = password;
  }
}